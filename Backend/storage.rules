rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Public read access, backend-only write
    match /{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}

